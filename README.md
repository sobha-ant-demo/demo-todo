# Demo Todo

A tiny Next.js (App Router) + Tailwind todo app used for demo purposes.

```bash
npm install
npm run dev
# http://localhost:3000
```

## Known issues

See the GitHub issues on this repo:

- **Bug:** the "items left" counter is wrong.
- **Feature request:** persist todos to `localStorage` so they survive a refresh.
